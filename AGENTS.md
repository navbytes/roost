# roost — for agents

The orientation for this repo is [CLAUDE.md](CLAUDE.md): specs of record, the
chrome rule, how tests and lints run, how a release is cut. Any tool that looks
for `AGENTS.md` should follow this pointer and read that file.

This used to be a machine translation of CLAUDE.md. It had already drifted —
it renamed the product "Codex Code" — and a second copy of a living document
drifts by construction. One orientation, one place.
